import React from 'react'
import ReactDOM from 'react-dom'

const DiagramEditor = React.createClass({
  propTypes: {

  },
  render: () => {
    return <p>
        Trying the config
      </p>
  }
})

ReactDOM.render(<DiagramEditor />, document.getElementById('app'))